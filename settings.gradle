pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        // MMD (com.mudita:MMD / MMD-android) is published to Maven Central.
        mavenCentral()
    }
}

rootProject.name = "kVexed"
include ':core'
include ':app'
